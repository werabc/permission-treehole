# 2026-09-22 代码审查与回滚

## 做了什么
- 全盘审查另一 agent 的未提交改动（145 修改 + 20 新增，+4787/-730 行）
- 结论：批量注入 ASCII 注释横幅（70 Java + 51 前端文件），其中 `MenuController.java` 末尾被插入孤立 `/` 导致编译失败
- 混入的功能改动：AI 聊天模块（AiChatController + WebFlux 依赖）、前端 a11y/骨架屏/响应式改进、`permission_v2_full.sql`（建了新库 permission_admin_v2，与运行配置不一致，有风险）
- 回滚方案：全部改动先提交到分支 `backup/agent-changes-20260922`（commit 2d02e54）备份，main 恢复到稳定版 `54636ab release: 树洞v1.0`
- 回滚后完整性扫描全部通过（0 注释残留、0 损坏文件尾、工作区干净）

## 注意
- 本机 bash 无 Java/mvn 环境，无法本地编译验证；项目通过 Docker 部署（deploy-package/）
- 如需找回 agent 改动中的可用部分（如前端 a11y 改进），从 backup 分支 cherry-pick

## 追加（第二轮：清除注释+审查）
- 发现稳定版 54636ab 自身就含 56 个 Java 文件的"文件注解与作用说明"尾注块（历史 agent 提交的）+ components.d.ts 横幅
- 用 scripts/strip_annotation_blocks.py 清除 1896 行纯注释，diff 验证 0 行代码误删，提交 b3a51af
- components.d.ts 被 gitignore（unplugin 自动生成），已物理清理不入库
- 审查结论：无 SQL 注入（mapper 零 ${} 拼接）、无 v-html（无 XSS 面）、JWT 密钥走环境变量、th_like 有唯一键 uk_user_target、like_count 减法有 GREATEST 防负数
- 待改进项：ThPost/ThComment 无 @Transactional（当前单写场景无碍）；并发双赞会触发唯一键 500 而非优雅处理；Swagger /doc.html 生产环境建议关闭

## 追加（第三轮：升级方向评估）
- 全盘功能完备性审查，找到 9 项"半成品"硬证据（关键：mute_until/ban_until/violation_count/max_post_per_day/post_need_audit 等治理字段在 Java 层引用数全为 0；通知后端齐全但 C 端无列表页；零搜索；零 MultipartFile 上传；无删帖接口；举报不闭环；C 端仅 7 页且 components 目录为空）
- 产出 UPGRADE_PLAN.md（6 大方向 A-F + 三阶段路线）
- 从 backup 分支恢复 docs/数据库表结构_树洞v1.0.md（264行，18表说明）
- 用户选定 **A+B 第一阶段一起做** → 产出 docs/升级实施清单_A+B.md（8 批次执行顺序）+ 建了 8 个跟踪任务
- 下一步：从批次1（治理基础设施：th_sensitive_word 表 + SensitiveWordService/ThSettingReader/RateLimiter/ThUserGuard）开始

## 追加（第四轮：A+B 升级实施 + 权限系统审查）

### 已完成升级（3 个提交，均已通过编译/构建）
- d357d9d 后端治理闭环：th_sensitive_word / th_collect 表（migration_v1.1.0.sql）；SensitiveWordService（Trie 前缀树 + 内存缓存，L1 拦截 / L2 转审）；ThUserGuard（封号 / 禁言 / 违规计分 / 阈值自动处罚，全部写 th_user_log 留痕）；ThRateLimiter（Redis 自然日限额）；接入登录 / 发帖 / 评论 / 点赞 / 举报主链路；举报成立即下架 + 计分 + 通知举报人；新增搜索 / 收藏 / 删帖删评论 / 改密码 / 他人主页接口；SecurityConfig 公开读接口全部限定 GET（原来会连带放行 DELETE）
- 7a98dba C 端：通知中心 / 搜索 / 我的收藏 / 删帖入口 / 账号设置 / 他人主页 + PostCard 公共组件
- af32247 管理端：敏感词管理页 + 违规分列 + 解除处罚；顺带修掉"封号到期后仍无法登录"的旧 bug
- 验证：mvn compile 通过；新增单测 24 个全绿（SensitiveWordServiceTest 11 + ThUserGuardTest 13）；treehole-web 与 permission-ui 均 npm run build 通过

### 工程环境要点（下次直接复用）
- JDK 路径：C:\Users\Lenovo\.jdks\ms-17.0.17
- bash 里跑 mvnw 会因项目路径含中文而 ClassNotFoundException，改用 PowerShell 直接调 java -classpath .mvn\wrapper\maven-wrapper.jar org.apache.maven.wrapper.MavenWrapperMain
- 跑指定测试需加 -Dsurefire.failIfNoSpecifiedTests=false（否则无匹配测试的模块会直接 FAILURE）

### 权限管理系统审查（docs/权限管理系统审查报告.md）
- P0：data_scope 完全未落地 —— 字段仅计算不参与任何查询，无 mapper/拦截器使用，sys_role_dept 表根本不存在 → 有 list 权限即可看全量数据
- P1：GET /api/user/{id}/roles 越权读；/api/auth/** 整段 permitAll 属结构性隐患；th-admin 菜单权限码全是粗粒度 'admin'，按钮级控制只覆盖 4 个系统页
- P2：SysUserServiceImplTest 4 个用例基线即失败（继承来的 baseMapper 无法被 Mockito 注入）→ "有测试"是假象

## 追加（第五轮：数据权限落地 + 深度全量测试）—— 提交 a2c0a01

### 数据权限（5 级 → 8 级，真正生效）
- DataScope：1全部 / 2本集团及以下 / 3本公司及以下 / 4本部门及以下 / 5本部门及以下(限N级) / 6本部门 / 7自定义部门 / 8仅本人
- migration_v1.2.0.sql：sys_role_dept 表、sys_dept.dept_level(带回填)、sys_role.data_scope_level；历史值必须【降序】重映射(5→8,4→7,3→6,2→4)否则互相覆盖
- 登录期预计算 LoginUser.deptIds(精确可见) 与 deptTreeIds(可见+祖先)；后者只用于 sys_dept，避免放大 sys_user 可见范围(越权)
- MyBatis-Plus DataPermissionInterceptor + MultiDataPermissionHandler(在 permission-system，config 在 framework，用 ObjectProvider 注入避免反向依赖)
- 管理端：角色页 8 级下拉 + 自定义部门树 + N 级输入；部门页显示层级；内置角色禁止删除/改编码

### 真实环境端到端测试（scripts/e2e_test.sh + sql/e2e_seed.sql，77 用例全绿）
- 环境要点：本机 MySQL 9.7(3306,root/123456abc)、Redis(6379,redis123456)、JDK 在 C:\Users\Lenovo\.jdks\ms-17.0.17
- Git Bash 下 curl -d '中文' 会弄坏 UTF-8 字节（表现为 500 Invalid UTF-8）→ 一律 printf | curl --data-binary @-
- 本机 redis-cli 不支持 --no-auth-warning；验证码值带 JSON 引号需 tr -d '"'
- 管理端登录限流按 IP，跑多账号用例需给每次登录不同的 X-Forwarded-For
- 启动后端：先 mvn -B install（生成 fat jar，且 spring-boot:run -pl 在聚合工程上会找不到主类），再 java -jar 并重定向日志到文件（用 Select-String 截断管道会杀掉 java 进程）

### 本轮发现并修复的 7 个缺陷（2 个 P0）
1. th_like 缺 update_time 列 → 点赞 100% 500（历史遗留，建表+迁移补列）
2. ThReportAdminController 绕过 Service 自写旧逻辑 → 举报闭环(下架/计分/通知)形同虚设
3. 数据权限过滤 sys_dept 导致父节点丢失 → 组织树整体空白（用 deptTreeIds 修复）
4. 改密后旧 Token 仍有效（filter 只认黑名单，删缓存无效）→ 改为拉黑；缓存值可能是 LoginUser 无法反序列化，改成从 JWT 解析 userId
5. GET /api/user/{id}/roles 越权读 → 补权限码
6. /api/auth/** 整段 permitAll → 收敛精确白名单
7. SysUserServiceImplTest 4 用例失效 → ReflectionTestUtils 注入父类 baseMapper

### 已知遗留（下一轮候选）
- 写操作(UPDATE/DELETE)未按数据范围校验（DataPermissionInterceptor 只改 SELECT）
- Token 吊销用 keys() 扫描，量大需优化为 user:{id}:tokens 集合
- 前端未做浏览器级 E2E（仅 build + 接口级验证）
- th-admin 权限码仍是粗粒度 admin，未拆细


