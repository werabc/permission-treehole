# 2026-09-24

## 本轮：接口文档 + 头像上传 + 树洞管理权限拆细

### 一、接口对账（scripts/api_audit.py）
- 后端 140 个端点，与 treehole-web（35 个调用）、permission-ui（101 个调用）双向比对
- **前端调用 → 后端：全部命中**，没有调用不存在的接口
- **后端 → 前端未调用**：`GET /api/th/post/{id}/liked`（真问题）、
  `PUT /api/admin/th/post/{id}/audit`（冗余，管理端走 batch-audit）、两个 /api/test 端点

### 二、挖出并修复的真缺陷（本轮 9 个）

| 级别 | 缺陷 | 说明 |
|---|---|---|
| 🔴 | `SecurityConfig` 的 `/api/admin/**` 是 `hasAuthority('admin')` 全局硬门 | **这才是"只有 admin 能管树洞"的根因**：非 admin 角色在进方法级鉴权前就被 403。改为 `authenticated()`，权限交给 @PreAuthorize |
| 🔴 | 帖子详情 `authorName` 恒为 null | 详情接口直接用 `getById`，只手工处理匿名分支 → **详情页一直显示"未知用户"**，分类标签也不显示。改为走 `getPostDetail` 复用列表填充逻辑 |
| 🟠 | 评论只能点赞、无法取消 | 后端缺 `DELETE /comment/{id}/like`，前端只能本地加数 → 数字越点越多。已补端点 + Service |
| 🟠 | 前端从不调 `/post/{id}/liked` | 刷新后"已抱抱"状态丢失；此时点击走 likePost（服务端幂等直接返回）前端却仍 +1 → 计数漂移。已接入 |
| 🟠 | multipart 默认上限 1MB < 头像上限 2MB | 1~2MB 图片被容器拦成 **500**。补齐 `spring.servlet.multipart.max-file-size=10MB` + `MaxUploadSizeExceededException` 兜底成 400 |
| 🟠 | 图片 Content-Type 被追加 `;charset=UTF-8` | `server.servlet.encoding.force=true` 强制给所有响应加 charset。手写响应流 + `response.reset()` 解决 |
| 🟠 | 404 被伪装成 401 | `sendError` 触发 ERROR 派发到 `/error`，而 `/error` 未放行 → 匿名请求的错误状态被改写。放行 `/error` + 文件 404 改用 `setStatus` |
| 🟡 | `th_category.post_count` 从未维护 | 分类数字冻结，前端显式对不上（上一轮已修） |
| 🟡 | 演示数据依赖"库里恰好有人注册过" | e2e_demo_seed 只 UPDATE id=1/2 → 干净库下作者缺失。改为自带 demo_night/demo_deer 账号 |

### 三、新增能力

**头像上传（全链路）**
- 后端：`FileStorageService` + `LocalFileStorageService`（permission-framework/storage）
  - 魔数判类型（不信 Content-Type/扩展名）、**明确拒绝 SVG**（内嵌 script = 存储型 XSS）
  - 服务端生成 UUID 文件名，客户端文件名一律丢弃 → 从根上消除路径穿越
  - 读取侧 category 白名单 + 文件名严格正则 + 归一化后确认在根目录内
  - `POST /api/file/avatar`（需登录）、`GET /api/file/{category}/{filename}`（公开，<img> 不带 Authorization）
  - `ThProfileDTO.avatar` 加格式白名单，只允许本站上传地址或 http(s) 外链（挡 `javascript:`/`data:`）
- 前端：`AppAvatar.vue` 统一组件（有头像用 img，无则首字母，匿名用遮罩图标）
  - Settings 改为真实上传控件（预览 + 类型/大小预校验）；顶栏 chip / PostCard / 帖子详情 / 评论 / 个人主页 / 他人主页全部接入
  - **匿名帖与匿名评论的 authorAvatar 恒为空**（头像可反向识别身份）
- 上线注意：`FILE_STORAGE_DIR` 要指向独立数据盘，别放发布目录；演示账号 th_auditor/th_operator 初始密码与种子 admin 相同（Admin@1234），**上线前必须改密或删除**

**树洞管理权限拆细（50 个端点）**
- 权限码：`th:user:*` / `th:post:*` / `th:comment:*` / `th:report:*` / `th:moderation:*` /
  `th:category:*` / `th:announcement:*` / `th:sensitive:*` / `th:online:*` / `th:analytics:view` /
  `th:settings:*` / `th:logs:view` / `system:dashboard:view`
- 一律写成 `hasAnyAuthority('<细码>', 'admin')`，**admin 保持全通**（向后兼容）
- `migration_v1.3.0.sql`：12 个菜单 perms 改为细码 + 29 个按钮级权限 + 2 个示例角色
  （内容审核员 th_auditor / 树洞运营 th_operator）+ 演示账号
- 管理端 8 个页面注入 `v-permission`（共 41 处）；**带 v-if/v-else 的按钮不能挂 v-permission**
  （指令在 mounted 直接摘 DOM，会破坏 v-else 配对）→ 改用 `can('code') && 原条件`

### 四、验证（全部真实环境）
- 后端 `mvn install`：61 个单测全绿
- 新增 `scripts/verify_avatar_perm.sh`：**65/65 全绿**（头像上传+恶意文件+穿越+权限矩阵+点赞态+匿名脱敏）
- 浏览器 `scripts/verify_treehole_ui.cjs`：**76/76 全绿**，控制台错误 0、4xx/5xx 0
- 接口文档 `docs/接口文档.md`：**140 个端点**，由 `scripts/gen_api_doc.py` 从源码生成（含附录 A–E：
  错误码、权限码表、头像上传、数据权限、本版接口变更与对账结论）

### 五、踩坑（复用）
- **Java 注解顺序**：`@GetMapping` 在 `@PreAuthorize` **之前**。用脚本批量改权限码时若"往前找"，
  会命中上一个方法的注解 → 权限码整体错位一位。必须"从 mapping 往后找、到 public 前为止"
- **Spring Security + sendError**：`sendError(404)` 会内部转发到 `/error`，`/error` 未放行则最终返回 401
- **`server.servlet.encoding.force=true`** 会给所有响应（含图片）追加 `;charset=UTF-8`
- **Jackson `non_null`**：null 字段整个键都不出现，断言要用 `.get()` 读
- Git Bash 的 `mktemp -d` 生成 `/tmp/...`，curl `--data-binary @/tmp/...` 读不到文件（MSYS 路径转换）
  → 临时文件放项目内
- 浏览器测试的账号/通知断言不能依赖历史残留数据：th_user 无种子数据，**用户都由注册接口产生**；
  通知必须现场造（另一用户点赞我的帖子）

---

## 追加：角色管理确认 + 4 项快修 + 验证状态标记

### 回答「admin 能不能建低权限角色、给用户分配」
**能，一直都有**：`POST /api/role`（建角色）、`PUT /api/role/{id}/menus`（配权限）、
`PUT /api/user/{id}/roles`（给用户分配角色），管理端有 system/role、system/user 页面。
上一轮拆细的 50 个树洞权限码让这件事真正可用了——以前建了角色也只挂 admin 一个码。

### 本轮 4 项快修
1. **createRole 显式禁内置编码**（提权加固）：原靠唯一性+admin不可删间接保护，
   若库被直接删掉 admin 角色再从界面重建就能造超管。入口封死 `BUILTIN_CODES`。
2. **Token 吊销 KEYS → SCAN**：`ThUserServiceImpl` 改密吊销用 `redisTemplate.keys()` 全库阻塞扫描，
   Token 量大时改密接口 = DoS 入口。改 SCAN 游标（count=500）。
3. **登录页浅色引语暗角**：`.auth__quote::before` radial 暗角 + blur，深色主题降到 0.3 透明度。
4. **删除 TestController**：遗留调试端点（/api/test/ping、admin-ping），无脚本依赖。

### 验证状态标记（避免重复测试）
- `docs/接口文档.md` 新增**附录 F：验证状态总览**——已验证模块标 ✅（含验证依据/日期），
  约定「代码未触及不重测」；本轮快修标注「编译通过、未跑 E2E、下次顺带覆盖」
- 接口文档重新生成：138 端点（删 TestController 少 2 个）

### 待升级清单（现状报告用）
写操作数据范围校验（拦截器只改写 SELECT）、管理端浏览器 E2E 重写、C 端 tsconfig 类型检查

## 用户视角 DOM 巡检 + 缺陷修复（本轮收尾）
用户要求「查前端 DOM 加载是否正常、按钮是否正常，从用户角度测试」。

### 新增两套巡检脚本（真实浏览器，非状态码）
- `scripts/audit_dom_user_view.cjs` —— C 端：白屏/关键文案/按钮可见可点/破图(naturalWidth===0)/
  横向溢出/JS 报错，覆盖首页、搜索、详情、发布、消息、我的、登录、注册 + 主题切换 + 关键交互可点性。**73/73**
- `scripts/audit_dom_admin.cjs` —— 管理端逐 hash 路由：登录页 + 8 系统页 + 12 树洞管理页。**120/120**
- 关键坑：hash 路由必须用 `/#/xxx`（path 模式会静默落到 dashboard）；登录页断言用「权限管理系统」；
  C 端 dev server 只监听 IPv6 `[::1]:3000`，curl 127.0.0.1 会 502。

### 巡检揪出的 3 个真缺陷（全部已修）
1. **首页 3 张破图**：注册时 `setAvatar("default.png")` 写占位串，不是可访问 URL。
   修：后端改 `setAvatar(null)`、DDL 默认值改 NULL、`AppAvatar.vue` 加 URL 白名单校验 + `@error` 兜底。
2. **管理端登录页横向溢出 800px**：`.login-bg` 宽 3200px 无裁切。修：`.login-container{overflow:hidden}`。
3. **C 端登录页浅色引语对比度不足**：加暗角径向渐变。

### 复跑结果（提交 dda1493）
C 端功能 76/76、C 端 DOM 73/73、管理端 DOM 120/120、管理端角色流程 10/10，控制台错误全 0。
附录 F 已补齐上述 4 行验证记录。

## 系统性补齐写侧数据权限（提交 463f693）

用户要求「补全问题，不要补洞式，要系统性，一次性解决同类问题」。

### 根因（一类问题，不是单点）
`DataPermissionInterceptor` 只改写 SELECT → **整个写侧没有门**。不是某个接口漏了，
而是 UPDATE/DELETE 从来就不在数据权限机制的覆盖范围内。表现为：
本部门管理员可重置超管密码 / 给他人分配 admin 角色 / 禁用删除范围外用户 / 改删范围外部门。

### 方案（读写同源）
- `DataScopeHelper` 新增 `canWriteUser` / `canWriteDept` 纯函数，**复用读侧 buildCondition 的语义**
  - 写侧 sys_user 判定只用 `deptIds`（精确可见部门），**不能用 `deptTreeIds`（含祖先）**，否则放大写权限
  - 写侧 sys_dept 用 `deptTreeIds`（与读侧一致，树结构要能管父节点）
- 新增 `DataScopeGuard`（`system/support/`）Service 层守门员，读写两侧统一入口
- 用户写侧 7 处插桩、部门写侧 3 处 + 父节点归属；超管 id=1 除本人外禁改禁删禁重置

### ⚠️ 踩到的关键坑（下次直接复用结论）
`DataPermissionHandler.getSqlSegment` **忽略了 mappedStatementId**，
所以它给**所有**命中白名单表的 SELECT 追加条件，包括 Mapper 里手写的"原生"SQL。
后果：守门员用来区分 404/403 的 `selectRawById` 也被改写，`selectRawById` 恒返回 0 行 →
所有越权都被静默降级成"用户不存在(404)"，看起来像拒绝了，实际语义全错。
**修复**：按 mappedStatementId 方法名末段**精确**豁免（不做前缀匹配，防 `selectRawByIdList` 误放行）：
`selectRawById` / `selectRoleCodesByUserId` / `selectPermissionsByUserId`。

### 验证（拒绝 + 不误伤，必须成对）
- `scripts/verify_write_scope.cjs` 26/26（越权写全 5002；范围内写 200；超管保护生效）
- `scripts/verify_guard_no_overreach.cjs` 16/16（admin 仍可管他人、主管可管本部门、自己能改自己）
- 单测：DataScopeWriteGuardTest（42 组合读写一致性矩阵）+ DataPermissionBypassTest（11）→ 模块 84/84
- 回归：C 端 76/76、管理端 DOM 120/120、用户视角 DOM 73/73、角色 UI 10/10，全无退化

### 环境坑（务必记住）
- **`SERVER__PORT=62748` 是 WorkBuddy 注入的环境变量**，Spring Boot 会把它映射成 `server.port`
  并**覆盖 yml**！启动必须显式 `--server.port=8081`，否则报 "Port 62748 already in use"（实际是抢 WorkBuddy 自己的端口）
- 沙箱里 `node` 无法 spawn 子进程 → 取验证码用 `net` 模块内联 Redis RESP 客户端
- 打包前必须先杀占用 jar 的后端进程，否则 repackage 报 "Unable to rename ... .jar.original"
