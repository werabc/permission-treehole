# 树洞平台 · 升级方向评估报告

> 审查时间：2026-09-22 | 基线版本：`b3a51af`（= 稳定发布版 54636ab + 注释清理）
> 审查范围：后端（Spring Boot 3.2 · 19 个控制器）、管理端（permission-ui）、C 端（treehole-web · 7 个页面）、数据库（18 张表）

---

## 一、为什么说它是"半成品"——硬证据

以下结论全部来自代码检索，不是推测：

| # | 缺口 | 证据 | 影响 |
|---|------|------|------|
| 1 | **治理体系"有表无实现"** | `th_user.mute_until` / `ban_until` / `violation_count`、`th_setting` 的 `max_post_per_day` / `max_comment_per_day` / `post_need_audit` / `comment_need_audit` —— **Java 层引用数全部为 0** | 禁言、封号、发帖限额、内容审核开关全是空字段；社区无法治理 |
| 2 | **通知中心只有一半** | 后端已有 3 个接口（列表 / 未读数 / 标记已读），C 端只调了"未读数"，**没有通知列表页、没有路由** | 用户看到红点却点不进去，"谁评论了我"看不见 |
| 3 | **搜索功能完全缺失** | 后端 0 个 search 接口，前端 0 个搜索入口 | 内容一多就无法检索 |
| 4 | **零文件上传能力** | 全项目 `MultipartFile` 引用数 = 0 | 头像只能用外链、帖子不能配图、公告不能上图 |
| 5 | **作者管不了自己的内容** | `ThPublicController` 只有 `DELETE /post/{id}/like`，**没有删除/编辑自己帖子的接口** | 发错内容撤不回 |
| 6 | **举报不闭环** | `ThReportServiceImpl.handleReport()` 只改 status + result：不通知举报人、不处罚被举报人、不累计 `violation_count` | 举报处理完没人知道，治理形同虚设 |
| 7 | **C 端只有 7 个页面** | Home / Login / Register / Publish / PostDetail / Category / Profile；`src/components/` **目录为空** | 无复用组件，功能面窄 |
| 8 | **账号安全不完整** | `ThAuthController` 仅 register / login / logout / user-info | 无改密码、无找回密码、无邮箱验证 |
| 9 | **社交关系为零** | 无关注/粉丝/收藏表 | 只有"内容流"，没有"关系链" |

> 结论：后端**数据模型设计得相当完整**（字段、配置项、管理端页面都齐），但**业务实现只做了主干（发帖-评论-点赞）**，外围治理、通知、检索、媒体全部落空。这正是典型的"骨架完整、血肉不足"。

---

## 二、六个升级方向（按建议优先级排序）

### 🅰 内容治理闭环落地（P0 · 强烈建议优先）

**把已有字段/配置接上真实逻辑，不加新表也能做完。**

- 敏感词过滤：发帖 / 评论入库前扫描，命中即拦截或转待审（词库入 `th_setting` 或独立表）
- 频率限制：`max_post_per_day` / `max_comment_per_day` 真正生效（Redis 计数器，按 `th_user_log` 统计）
- 禁言/封号生效：`mute_until` 拦发帖评论，`ban_until` 拦登录，`violation_count` 自动计分
- 审核开关：`post_need_audit=1` 时帖子进待审队列，管理端已有 `moderation` 页面承接
- 举报闭环：处理时 → 通知举报人 → 处罚作者 → 写违规记录 → 累计 count
- 自动处罚策略：违规分 ≥ N 自动禁言 X 天

**收益**：从"能发帖的 Demo"变成"可对外开放运营的社区"。**工作量**：中（无需改表结构，主要是 Service 层 + 少量管理端联动）

---

### 🅱 C 端功能补全（P0 · 用户可感知）

**后端大多已就绪，主要是前端补齐 + 少量接口。**

- **消息通知中心页**：列表 + 未读红点 + 点击跳转 + 全部已读（**后端 3 个接口现成，纯前端工作，性价比最高**）
- **搜索**：帖子/作者/分类搜索，后端加 `GET /api/th/search`，前端加搜索框 + 结果页
- **我的内容管理**：删除自己的帖子/评论（+ 可选 30 分钟内编辑）
- **我的收藏**：新增 `th_collect` 表 + 卡片收藏按钮 + Profile 新页签
- **他人主页**：点昵称进入 TA 的公开主页（复用 Profile 组件，只读模式）
- **用户设置页**：改密码、完善资料（`th_user` 的 avatar/bio/gender/email 字段都空着没入口）

**收益**：产品"像个产品"，用户留得住。**工作量**：中（通知中心是低垂果实）

---

### 🅲 媒体与富内容（P1）

- 文件上传服务（本地存储 + 可切 OSS/COS），含类型白名单、大小限制、图片压缩
- 头像上传、帖子配图（多图九宫格）、公告封面
- 富文本/Markdown 支持与安全过滤（当前无 `v-html`，接富文本时要防 XSS）
- 图片懒加载 + CDN 前缀

**收益**：表达力大幅提升（纯文字树洞吸引力有限）。**工作量**：中高（涉及存储方案选型）

---

### 🅳 账号安全与身份体系（P1）

- 修改密码、忘记密码（邮箱/短信验证码重置）
- 邮箱验证、第三方登录（微信/QQ）可选
- 登录设备日志（`th_user_log` 已有表，接上即可）
- 注册风控：图形验证码（管理端已有 Hutool 验证码实现，可复用）

**收益**：账号可自服务，减少运营人工介入。**工作量**：中

---

### 🅴 社交关系与实时化（P2）

- 关注 / 粉丝、TA 的关注动态流
- @提及 + 通知联动
- WebSocket / SSE 实时未读推送（替代轮询）
- 楼中楼评论、评论点赞通知补全

**收益**：从"信息流"升级成"社区"，显著提留存。**工作量**：高

---

### 🅵 工程质量与交付（P2 · 建议并行做）

- 抽 C 端公共组件库（当前 `components/` 为空，页面代码重复度高）
- Service 层单元测试补齐（后端目前仅 2 个测试类）；治理逻辑上线前必须有测试兜底
- CI 流水线（构建 + 测试 + 前端 lint/build），防止再次出现"agent 改坏代码没人发现"
- 生产配置收口：关闭 Swagger（`/doc.html`）、`JWT_SECRET_KEY` 强制校验、日志脱敏
- 数据看板增强（管理端 analytics 已有基础，可补 DAU/留存/内容趋势）

**收益**：降低后续开发风险（这次就被无审查的批量改动坑过）。**工作量**：中

---

## 三、推荐路线

```
第一阶段（让产品可用）：🅰 内容治理闭环  →  🅱 C 端补全（先做通知中心 + 删帖 + 我的收藏）
第二阶段（让产品有料）：🅲 媒体上传      →  🅳 账号安全
第三阶段（让产品有黏性）：🅴 社交关系与实时化
贯穿始终：            🅵 工程质量（测试 + CI + 生产配置）
```

**如果只挑一件先做**：🅰 治理闭环 —— 因为数据字段和配置项都现成，做完就能真上线运营；同时 🅱 里的「通知中心」是半天内见效的低垂果实，可以和 🅰 并行。

---

## 四、附：本次审查顺带修复/恢复的内容

- 恢复 `docs/数据库表结构_树洞v1.0.md`（264 行，18 张表逐表说明，之前被回滚连带移出工作区，升级设计新表时需要它）
- 前两轮已完成：回滚损坏改动 → 清除 1896 行噪音注释 → 全盘安全审查（无 SQL 注入 / 无 XSS 面）
