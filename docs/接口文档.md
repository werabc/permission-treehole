# 接口文档

> 本文件由 `scripts/gen_api_doc.py` 依据 Controller 源码生成，请勿手工编辑端点表格。
> 当前共 **138** 个端点。

## 通用约定

### 统一响应结构

```json
{ "code": 200, "message": "success", "data": {} }
```

- `code=200` 表示业务成功；非 200 时 `data` 通常为空，`message` 为可直接展示给用户的提示。
- **部分业务失败也返回 HTTP 200 + 业务码**（如"无权操作"是 `code=5002` 而不是 HTTP 403），
  前端判断必须以 `code` 为准，不要只看 HTTP 状态码。
- Jackson 开启了 `non_null`：**为 null 的字段不会出现在 JSON 里**。
  前端读取可选字段请用 `data.field` 之前先判空，或统一按"字段缺失 = 无值"处理。

### 分页

列表接口统一入参 `pageNum`（从 1 开始）、`pageSize`（服务端上限 100，超出会被截断）。
返回结构：

```json
{ "records": [], "total": 0, "size": 10, "current": 1, "pages": 0 }
```

### 鉴权

| 端类型 | 说明 |
|---|---|
| 公开 | 无需 Token，如列表 / 详情 / 搜索 / 头像读取 |
| 需登录 | 请求头 `Authorization: Bearer <token>` |
| 需权限码 | 除登录外还要命中注解里的权限码；表里列出全部可接受码，命中任意一个即通过 |

管理端与树洞端**各自签发 Token，互不通用**：管理端 `/api/auth/login`，树洞端 `/api/th/auth/login`。

`admin` 角色码是超级权限，在所有 `hasAnyAuthority` 判断里都放行，用于向后兼容。

---

## 端点总览

| 控制器 | 说明 | 端点数 | 路径前缀 |
|---|---|---|---|
| `AuthController` | 认证管理 | 5 | `/api/auth` |
| `ThAuthController` | 树洞认证 | 5 | `/api/th/auth` |
| `FileController` | 文件 | 2 | `/api/file` |
| `ThPublicController` | 树洞公共接口 | 29 | `/api/th` |
| `DashboardController` | 仪表盘 | 7 | `/api/dashboard` |
| `ThAnalyticsController` | 树洞数据分析 | 3 | `/api/admin/th/analytics` |
| `ThModerationController` | 树洞内容审核 | 4 | `/api/admin/th/moderation` |
| `ThPostAdminController` | 树洞帖子管理 | 6 | `/api/admin/th/post` |
| `ThCommentAdminController` | 树洞评论管理 | 4 | `/api/admin/th/comment` |
| `ThReportAdminController` | 树洞举报管理 | 5 | `/api/admin/th/report` |
| `ThUserAdminController` | 树洞用户管理 | 11 | `/api/admin/th/user` |
| `ThCategoryAdminController` | 树洞分类管理 | 4 | `/api/admin/th/category` |
| `ThAnnouncementController` | 树洞公告管理 | 4 | `/api/admin/th/announcement` |
| `ThSensitiveWordAdminController` | 树洞敏感词管理 | 7 | `/api/admin/th/sensitive-word` |
| `ThSettingsController` | 树洞站点配置 | 2 | `/api/admin/th/settings` |
| `OnlineUserController` | 在线用户 | 3 | `/api/online` |
| `UserController` | 用户管理 | 13 | `/api/user` |
| `RoleController` | 角色管理 | 9 | `/api/role` |
| `MenuController` | 菜单管理 | 7 | `/api/menu` |
| `DeptController` | 部门管理 | 6 | `/api/dept` |
| `LogController` | 日志管理 | 2 | `/api/log` |

---

## 认证管理

`AuthController` · 前缀 `/api/auth`

| 方法 | 路径 | 说明 | 权限码 | 参数 | 返回 |
|---|---|---|---|---|---|
| GET | `/api/auth/captcha` | 获取验证码 | 公开 | - | `R<Map<String, Object>>` |
| POST | `/api/auth/login` | 用户登录 | 公开 | `loginDTO（body，LoginDTO）` | `R<TokenVO>` |
| POST | `/api/auth/refresh` | 刷新Token | 公开 | `body（body，Map<String, String>）` | `R<TokenVO>` |
| POST | `/api/auth/logout` | 退出登录 | 仅登录 | - | `R<Void>` |
| GET | `/api/auth/user-info` | 获取当前用户信息 | 仅登录 | `loginUser（登录用户，由框架注入）` | `R<Map<String, Object>>` |

## 树洞认证

`ThAuthController` · 前缀 `/api/th/auth`

| 方法 | 路径 | 说明 | 权限码 | 参数 | 返回 |
|---|---|---|---|---|---|
| POST | `/api/th/auth/register` | 注册 | 公开 | `loginDTO（body，LoginDTO）` | `R<Map<String, String>>` |
| POST | `/api/th/auth/login` | 登录 | 公开 | `loginDTO（body，LoginDTO）` | `R<Map<String, String>>` |
| POST | `/api/th/auth/logout` | 退出登录 | 公开 | - | `R<Void>` |
| PUT | `/api/th/auth/password` | 修改密码 | 公开 | `body（body，Map<String, String>）` | `R<Void>` |
| GET | `/api/th/auth/user-info` | 获取当前用户信息 | 公开 | - | `R<Map<String, Object>>` |

## 文件

`FileController` · 前缀 `/api/file`

| 方法 | 路径 | 说明 | 权限码 | 参数 | 返回 |
|---|---|---|---|---|---|
| POST | `/api/file/avatar` | 上传头像 | 仅登录 | `file（multipart/form-data 文件字段）` | `R<String>` |
| GET | `/api/file/{category}/{filename}` | 读取文件 | 公开 | `category（路径变量）`<br>`filename（路径变量）` | `void` |

## 树洞公共接口

`ThPublicController` · 前缀 `/api/th`

| 方法 | 路径 | 说明 | 权限码 | 参数 | 返回 |
|---|---|---|---|---|---|
| GET | `/api/th/category/list` | 分类列表 | 公开 | - | `R<List<ThCategory>>` |
| GET | `/api/th/announcements` | 树洞公告列表（公开） | 公开 | - | `R<List<ThAnnouncement>>` |
| GET | `/api/th/post/page` | 帖子列表 | 公开 | `pageNum（query，可选，默认 1）`<br>`pageSize（query，可选，默认 10）`<br>`categoryId（query，可选）`<br>`keyword（query，可选）` | `R<IPage<ThPost>>` |
| GET | `/api/th/post/{id}` | 帖子详情 | 公开 | `id（路径变量）`<br>`loginUser（登录用户，由框架注入）` | `R<ThPost>` |
| GET | `/api/th/post/{id}/liked` | 检查是否点赞 | 公开 | `id（路径变量）`<br>`loginUser（登录用户，由框架注入）` | `R<Boolean>` |
| POST | `/api/th/post` | 创建帖子 | 仅登录 | `post（body，ThPost）`<br>`loginUser（登录用户，由框架注入）` | `R<Long>` |
| POST | `/api/th/post/{id}/like` | 点赞帖子 | 仅登录 | `id（路径变量）`<br>`loginUser（登录用户，由框架注入）` | `R<Void>` |
| DELETE | `/api/th/post/{id}/like` | 取消点赞帖子 | 仅登录 | `id（路径变量）`<br>`loginUser（登录用户，由框架注入）` | `R<Void>` |
| GET | `/api/th/comment/page` | 评论列表 | 公开 | `pageNum（query，可选，默认 1）`<br>`pageSize（query，可选，默认 10）`<br>`postId（query，必填）`<br>`loginUser（登录用户，由框架注入）` | `R<IPage<ThComment>>` |
| POST | `/api/th/comment` | 创建评论 | 仅登录 | `comment（body，ThComment）`<br>`loginUser（登录用户，由框架注入）` | `R<Long>` |
| POST | `/api/th/comment/{id}/like` | 点赞评论 | 仅登录 | `id（路径变量）`<br>`loginUser（登录用户，由框架注入）` | `R<Void>` |
| DELETE | `/api/th/comment/{id}/like` | 取消点赞评论 | 仅登录 | `id（路径变量）`<br>`loginUser（登录用户，由框架注入）` | `R<Void>` |
| POST | `/api/th/report` | 提交举报 | 仅登录 | `report（body，ThReport）`<br>`loginUser（登录用户，由框架注入）` | `R<Void>` |
| DELETE | `/api/th/post/{id}` | 删除自己的帖子 | 仅登录 | `id（路径变量）`<br>`loginUser（登录用户，由框架注入）` | `R<Void>` |
| DELETE | `/api/th/comment/{id}` | 删除自己的评论 | 仅登录 | `id（路径变量）`<br>`loginUser（登录用户，由框架注入）` | `R<Void>` |
| GET | `/api/th/search` | 搜索帖子（内容 + 作者昵称） | 公开 | `keyword（query，必填）`<br>`pageNum（query，可选，默认 1）`<br>`pageSize（query，可选，默认 10）` | `R<IPage<ThPost>>` |
| POST | `/api/th/collect/{postId}` | 切换收藏状态 | 仅登录 | `postId（路径变量）`<br>`loginUser（登录用户，由框架注入）` | `R<Boolean>` |
| GET | `/api/th/post/{id}/collected` | 是否已收藏 | 公开 | `id（路径变量）`<br>`loginUser（登录用户，由框架注入）` | `R<Boolean>` |
| GET | `/api/th/user/collects` | 我收藏的帖子 | 仅登录 | `pageNum（query，可选，默认 1）`<br>`pageSize（query，可选，默认 10）`<br>`loginUser（登录用户，由框架注入）` | `R<IPage<ThPost>>` |
| GET | `/api/th/user/collect-count` | 收藏数量 | 仅登录 | `loginUser（登录用户，由框架注入）` | `R<Long>` |
| GET | `/api/th/user/public/{id}` | 用户公开主页（不含隐私字段） | 公开 | `id（路径变量）` | `R<Map<String, Object>>` |
| GET | `/api/th/user/{id}/posts` | 指定用户的公开帖子 | 公开 | `id（路径变量）`<br>`pageNum（query，可选，默认 1）`<br>`pageSize（query，可选，默认 10）` | `R<IPage<ThPost>>` |
| GET | `/api/th/user/posts` | 我的帖子 | 仅登录 | `pageNum（query，可选，默认 1）`<br>`pageSize（query，可选，默认 10）`<br>`loginUser（登录用户，由框架注入）` | `R<IPage<ThPost>>` |
| GET | `/api/th/user/my-comments` | 我的评论 | 仅登录 | `pageNum（query，可选，默认 1）`<br>`pageSize（query，可选，默认 10）`<br>`loginUser（登录用户，由框架注入）` | `R<IPage<ThComment>>` |
| GET | `/api/th/user/received-comments` | 收到的评论 | 仅登录 | `pageNum（query，可选，默认 1）`<br>`pageSize（query，可选，默认 10）`<br>`loginUser（登录用户，由框架注入）` | `R<IPage<ThComment>>` |
| GET | `/api/th/user/notifications` | 通知列表 | 仅登录 | `pageNum（query，可选，默认 1）`<br>`pageSize（query，可选，默认 10）`<br>`unreadOnly（query，可选）`<br>`loginUser（登录用户，由框架注入）` | `R<IPage<ThNotification>>` |
| GET | `/api/th/user/unread-count` | 未读通知数量 | 仅登录 | `loginUser（登录用户，由框架注入）` | `R<Long>` |
| PUT | `/api/th/user/notifications/read` | 标记通知已读 | 仅登录 | `body（body，Map<String, List<Long>>）`<br>`loginUser（登录用户，由框架注入）` | `R<Void>` |
| PUT | `/api/th/user/profile` | 更新个人资料 | 仅登录 | `loginUser（登录用户，由框架注入）` | `R<Void>` |

## 仪表盘

`DashboardController` · 前缀 `/api/dashboard`

| 方法 | 路径 | 说明 | 权限码 | 参数 | 返回 |
|---|---|---|---|---|---|
| GET | `/api/dashboard/overview` | 综合仪表盘数据 | `system:dashboard:view / admin` | - | `R<Map<String, Object>>` |
| GET | `/api/dashboard/admin-stats` | 管理员系统统计 | `system:dashboard:view / admin` | - | `R<Map<String, Object>>` |
| GET | `/api/dashboard/treehole-stats` | 树洞系统统计 | `system:dashboard:view / admin` | - | `R<Map<String, Object>>` |
| GET | `/api/dashboard/pending` | 待处理事项 | `system:dashboard:view / admin` | - | `R<Map<String, Object>>` |
| GET | `/api/dashboard/trends` | 趋势数据 | `system:dashboard:view / admin` | `days（query，可选，默认 7）` | `R<Map<String, Object>>` |
| GET | `/api/dashboard/realtime` | 实时统计 | `system:dashboard:view / admin` | - | `R<Map<String, Object>>` |
| GET | `/api/dashboard/statistics` | 管理员系统统计(兼容旧接口) | `system:dashboard:view / admin` | - | `R<Map<String, Object>>` |

## 树洞数据分析

`ThAnalyticsController` · 前缀 `/api/admin/th/analytics`

| 方法 | 路径 | 说明 | 权限码 | 参数 | 返回 |
|---|---|---|---|---|---|
| GET | `/api/admin/th/analytics/overview` | 概览数据 | `th:analytics:view / admin` | - | `R<Map<String, Object>>` |
| GET | `/api/admin/th/analytics/trends` | 趋势数据 | `th:analytics:view / admin` | `days（query，可选，默认 7）` | `R<Map<String, Object>>` |
| GET | `/api/admin/th/analytics/categories` | 分类热度 | `th:analytics:view / admin` | - | `R<List<Map<String, Object>>>` |

## 树洞内容审核

`ThModerationController` · 前缀 `/api/admin/th/moderation`

| 方法 | 路径 | 说明 | 权限码 | 参数 | 返回 |
|---|---|---|---|---|---|
| GET | `/api/admin/th/moderation/posts` | 待审核帖子 | `th:moderation:list / admin` | `pageNum（query，可选，默认 1）`<br>`pageSize（query，可选，默认 20）` | `R<IPage<ThPost>>` |
| GET | `/api/admin/th/moderation/comments` | 待审核评论 | `th:moderation:list / admin` | `pageNum（query，可选，默认 1）`<br>`pageSize（query，可选，默认 20）` | `R<IPage<ThComment>>` |
| POST | `/api/admin/th/moderation/batch-audit` | 批量审核 | `th:moderation:audit / admin` | `body（body，Map<String, Object>）` | `R<Void>` |
| GET | `/api/admin/th/moderation/stats` | 审核统计 | `th:moderation:list / admin` | - | `R<Map<String, Long>>` |

## 树洞帖子管理

`ThPostAdminController` · 前缀 `/api/admin/th/post`

| 方法 | 路径 | 说明 | 权限码 | 参数 | 返回 |
|---|---|---|---|---|---|
| GET | `/api/admin/th/post/page` | 帖子列表 | `th:post:list / admin` | `pageNum（query，可选，默认 1）`<br>`pageSize（query，可选，默认 10）`<br>`status（query，可选）`<br>`categoryId（query，可选）`<br>`keyword（query，可选）` | `R<IPage<ThPost>>` |
| GET | `/api/admin/th/post/{id}` | 帖子详情 | `th:post:view / admin` | `id（路径变量）` | `R<Map<String, Object>>` |
| PUT | `/api/admin/th/post/{id}/audit` | 审核帖子 | `th:post:audit / admin` | `id（路径变量）`<br>`status（query，必填）`<br>`auditRemark（query，可选）`<br>`loginUser（登录用户，由框架注入）` | `R<Void>` |
| PUT | `/api/admin/th/post/{id}/pin` | 置顶/取消置顶 | `th:post:pin / admin` | `id（路径变量）`<br>`isTop（query，必填）` | `R<Void>` |
| PUT | `/api/admin/th/post/{id}/hide` | 隐藏/恢复 | `th:post:hide / admin` | `id（路径变量）`<br>`status（query，必填）` | `R<Void>` |
| DELETE | `/api/admin/th/post/{id}` | 删除帖子 | `th:post:delete / admin` | `id（路径变量）` | `R<Void>` |

## 树洞评论管理

`ThCommentAdminController` · 前缀 `/api/admin/th/comment`

| 方法 | 路径 | 说明 | 权限码 | 参数 | 返回 |
|---|---|---|---|---|---|
| GET | `/api/admin/th/comment/page` | 评论列表 | `th:comment:list / admin` | `pageNum（query，可选，默认 1）`<br>`pageSize（query，可选，默认 10）`<br>`postId（query，可选）` | `R<IPage<ThComment>>` |
| GET | `/api/admin/th/comment/{id}` | 评论详情 | `th:comment:view / admin` | `id（路径变量）` | `R<Map<String, Object>>` |
| PUT | `/api/admin/th/comment/{id}/hide` | 隐藏/恢复评论 | `th:comment:hide / admin` | `id（路径变量）`<br>`status（query，必填）` | `R<Void>` |
| DELETE | `/api/admin/th/comment/{id}` | 删除评论 | `th:comment:delete / admin` | `id（路径变量）` | `R<Void>` |

## 树洞举报管理

`ThReportAdminController` · 前缀 `/api/admin/th/report`

| 方法 | 路径 | 说明 | 权限码 | 参数 | 返回 |
|---|---|---|---|---|---|
| GET | `/api/admin/th/report/page` | 举报列表 | `th:report:list / admin` | `pageNum（query，可选，默认 1）`<br>`pageSize（query，可选，默认 10）`<br>`status（query，可选）` | `R<IPage<ThReport>>` |
| GET | `/api/admin/th/report/{id}` | 举报详情 | `th:report:view / admin` | `id（路径变量）` | `R<Map<String, Object>>` |
| PUT | `/api/admin/th/report/{id}/handle` | 处理举报 | `th:report:handle / admin` | `id（路径变量）`<br>`status（query，必填）`<br>`result（query，可选）`<br>`loginUser（登录用户，由框架注入）` | `R<Void>` |
| POST | `/api/admin/th/report/batch-handle` | 批量处理 | `th:report:handle / admin` | `body（body，Map<String, Object>）`<br>`loginUser（登录用户，由框架注入）` | `R<Void>` |
| GET | `/api/admin/th/report/stats` | 举报统计 | `th:report:list / admin` | - | `R<Map<String, Long>>` |

## 树洞用户管理

`ThUserAdminController` · 前缀 `/api/admin/th/user`

| 方法 | 路径 | 说明 | 权限码 | 参数 | 返回 |
|---|---|---|---|---|---|
| GET | `/api/admin/th/user/page` | 用户列表 | `th:user:list / admin` | `pageNum（query，可选，默认 1）`<br>`pageSize（query，可选，默认 10）`<br>`keyword（query，可选）`<br>`status（query，可选）` | `R<IPage<ThUser>>` |
| GET | `/api/admin/th/user/{id}` | 用户详情 | `th:user:view / admin` | `id（路径变量）` | `R<Map<String, Object>>` |
| PUT | `/api/admin/th/user/{id}/mute` | 禁言用户 | `th:user:mute / admin` | `id（路径变量）`<br>`hours（query，可选）`<br>`days（query，可选）`<br>`reason（query，可选）`<br>`operator（登录用户，由框架注入）` | `R<Void>` |
| PUT | `/api/admin/th/user/{id}/unmute` | 取消禁言 | `th:user:mute / admin` | `id（路径变量）`<br>`operator（登录用户，由框架注入）` | `R<Void>` |
| PUT | `/api/admin/th/user/{id}/ban` | 封号 | `th:user:ban / admin` | `id（路径变量）`<br>`days（query，可选）`<br>`reason（query，可选）`<br>`operator（登录用户，由框架注入）` | `R<Void>` |
| PUT | `/api/admin/th/user/{id}/unban` | 解封 | `th:user:ban / admin` | `id（路径变量）`<br>`operator（登录用户，由框架注入）` | `R<Void>` |
| PUT | `/api/admin/th/user/{id}/release` | 解除全部处罚（可选清零违规分） | `th:user:release / admin` | `id（路径变量）`<br>`resetViolation（query，可选，默认 false）`<br>`operator（登录用户，由框架注入）` | `R<Void>` |
| PUT | `/api/admin/th/user/{id}/violation` | 手动加减违规分 | `th:user:violation / admin` | `id（路径变量）`<br>`score（query，必填）`<br>`reason（query，可选）` | `R<Integer>` |
| GET | `/api/admin/th/user/{id}/logs` | 用户行为日志 | `th:user:view / admin` | `id（路径变量）`<br>`pageNum（query，可选，默认 1）`<br>`pageSize（query，可选，默认 20）` | `R<IPage<ThUserLog>>` |
| GET | `/api/admin/th/user/{id}/posts` | 用户发帖记录 | `th:user:view / admin` | `id（路径变量）`<br>`pageNum（query，可选，默认 1）`<br>`pageSize（query，可选，默认 20）` | `R<IPage<com.permission.common.entity.ThPost>>` |
| GET | `/api/admin/th/user/{id}/comments` | 用户评论记录 | `th:user:view / admin` | `id（路径变量）`<br>`pageNum（query，可选，默认 1）`<br>`pageSize（query，可选，默认 20）` | `R<IPage<com.permission.common.entity.ThComment>>` |

## 树洞分类管理

`ThCategoryAdminController` · 前缀 `/api/admin/th/category`

| 方法 | 路径 | 说明 | 权限码 | 参数 | 返回 |
|---|---|---|---|---|---|
| GET | `/api/admin/th/category/list` | 分类列表(管理) | `th:category:list / admin` | - | `R<List<ThCategory>>` |
| POST | `/api/admin/th/category` | 新增分类 | `th:category:add / admin` | `category（body，ThCategory）` | `R<Void>` |
| PUT | `/api/admin/th/category/{id}` | 修改分类 | `th:category:edit / admin` | `id（路径变量）`<br>`category（body，ThCategory）` | `R<Void>` |
| DELETE | `/api/admin/th/category/{id}` | 删除分类 | `th:category:delete / admin` | `id（路径变量）` | `R<Void>` |

## 树洞公告管理

`ThAnnouncementController` · 前缀 `/api/admin/th/announcement`

| 方法 | 路径 | 说明 | 权限码 | 参数 | 返回 |
|---|---|---|---|---|---|
| GET | `/api/admin/th/announcement/page` | 公告列表 | `th:announcement:list / admin` | `pageNum（query，可选，默认 1）`<br>`pageSize（query，可选，默认 10）`<br>`type（query，可选）` | `R<IPage<ThAnnouncement>>` |
| POST | `/api/admin/th/announcement` | 创建公告 | `th:announcement:add / admin` | `announcement（body，ThAnnouncement）`<br>`loginUser（登录用户，由框架注入）` | `R<Long>` |
| PUT | `/api/admin/th/announcement/{id}` | 编辑公告 | `th:announcement:edit / admin` | `id（路径变量）`<br>`announcement（body，ThAnnouncement）` | `R<Void>` |
| DELETE | `/api/admin/th/announcement/{id}` | 删除公告 | `th:announcement:delete / admin` | `id（路径变量）` | `R<Void>` |

## 树洞敏感词管理

`ThSensitiveWordAdminController` · 前缀 `/api/admin/th/sensitive-word`

| 方法 | 路径 | 说明 | 权限码 | 参数 | 返回 |
|---|---|---|---|---|---|
| GET | `/api/admin/th/sensitive-word/page` | 敏感词分页列表 | `th:sensitive:list / admin` | `pageNum（query，可选，默认 1）`<br>`pageSize（query，可选，默认 20）`<br>`keyword（query，可选）`<br>`level（query，可选）`<br>`status（query，可选）` | `R<IPage<ThSensitiveWord>>` |
| GET | `/api/admin/th/sensitive-word/stats` | 词库统计（词条数 / 总开关状态） | `th:sensitive:list / admin` | - | `R<Map<String, Object>>` |
| POST | `/api/admin/th/sensitive-word` | 新增敏感词 | `th:sensitive:add / admin` | `word（body，ThSensitiveWord）`<br>`operator（登录用户，由框架注入）` | `R<Void>` |
| POST | `/api/admin/th/sensitive-word/import` | 批量导入（换行或逗号分隔） | `th:sensitive:import / admin` | `body（body，Map<String, Object>）`<br>`operator（登录用户，由框架注入）` | `R<Integer>` |
| PUT | `/api/admin/th/sensitive-word` | 修改敏感词 | `th:sensitive:edit / admin` | `word（body，ThSensitiveWord）` | `R<Void>` |
| DELETE | `/api/admin/th/sensitive-word/{id}` | 删除敏感词 | `th:sensitive:delete / admin` | `id（路径变量）` | `R<Void>` |
| POST | `/api/admin/th/sensitive-word/refresh` | 手动刷新词库缓存 | `th:sensitive:refresh / admin` | - | `R<Integer>` |

## 树洞站点配置

`ThSettingsController` · 前缀 `/api/admin/th/settings`

| 方法 | 路径 | 说明 | 权限码 | 参数 | 返回 |
|---|---|---|---|---|---|
| GET | `/api/admin/th/settings` | 获取所有配置 | `th:settings:view / admin` | - | `R<Map<String, String>>` |
| PUT | `/api/admin/th/settings` | 更新配置 | `th:settings:edit / admin` | `body（body，Map<String, String>）` | `R<Void>` |

## 在线用户

`OnlineUserController` · 前缀 `/api/online`

| 方法 | 路径 | 说明 | 权限码 | 参数 | 返回 |
|---|---|---|---|---|---|
| GET | `/api/online` | 获取在线用户列表 | `th:online:list / admin` | - | `R<List<Map<String, Object>>>` |
| GET | `/api/online/count` | 获取在线用户数 | 仅登录 | - | `R<Long>` |
| DELETE | `/api/online/{userId}` | 强制用户下线 | `th:online:kick / admin` | `userId（路径变量）` | `R<Void>` |

## 用户管理

`UserController` · 前缀 `/api/user`

| 方法 | 路径 | 说明 | 权限码 | 参数 | 返回 |
|---|---|---|---|---|---|
| GET | `/api/user/page` | 分页查询用户 | `system:user:list / admin` | `pageNum（query，可选，默认 1）`<br>`pageSize（query，可选，默认 10）`<br>`keyword（query，可选）`<br>`deptId（query，可选）`<br>`status（query，可选）` | - |
| GET | `/api/user/{id}` | 获取用户详情 | `system:user:query / admin` | `id（路径变量）` | `R<SysUser>` |
| POST | `/api/user` | 新增用户 | `system:user:add / admin` | `user（body，SysUser）` | `R<Void>` |
| PUT | `/api/user/{id}` | 修改用户 | `system:user:edit / admin` | `id（路径变量）`<br>`user（body，SysUser）` | `R<Void>` |
| DELETE | `/api/user/{ids}` | 删除用户 | `system:user:delete / admin` | `ids（路径变量）` | `R<Void>` |
| PUT | `/api/user/{id}/status` | 修改用户状态 | `system:user:edit / admin` | `id（路径变量）`<br>`body（body，Map<String, Integer>）` | `R<Void>` |
| PUT | `/api/user/{id}/reset-password` | 重置密码 | `system:user:reset-pwd / admin` | `id（路径变量）`<br>`body（body，Map<String, String>）` | `R<Void>` |
| PUT | `/api/user/update-password` | 修改个人密码 | 仅登录 | `loginUser（登录用户，由框架注入）`<br>`body（body，Map<String, String>）` | `R<Void>` |
| PUT | `/api/user/{id}/roles` | 分配角色 | `system:user:edit / admin` | `id（路径变量）`<br>`body（body，Map<String, Set<Long>>）` | `R<Void>` |
| GET | `/api/user/{id}/roles` | 获取用户角色ID列表 | `system:user:query / admin` | `id（路径变量）` | `R<Set<Long>>` |
| PUT | `/api/user/profile` | 修改个人信息 | 仅登录 | `loginUser（登录用户，由框架注入）`<br>`profileDTO（body，ProfileDTO）` | `R<Void>` |
| GET | `/api/user/export` | 导出用户列表 | `system:user:list / admin` | - | `void` |
| PUT | `/api/user/batch-status` | 批量修改用户状态 | `system:user:edit / admin` | `body（body，Map<String, Object>）` | `R<Void>` |

## 角色管理

`RoleController` · 前缀 `/api/role`

| 方法 | 路径 | 说明 | 权限码 | 参数 | 返回 |
|---|---|---|---|---|---|
| GET | `/api/role/page` | 分页查询角色 | `system:role:list / admin` | `pageNum（query，可选，默认 1）`<br>`pageSize（query，可选，默认 10）`<br>`keyword（query，可选）` | - |
| GET | `/api/role/{id}` | 获取角色详情 | `system:role:query / admin` | `id（路径变量）` | `R<SysRole>` |
| POST | `/api/role` | 新增角色 | `system:role:add / admin` | `role（body，SysRole）` | `R<Void>` |
| PUT | `/api/role/{id}` | 修改角色 | `system:role:edit / admin` | `id（路径变量）`<br>`role（body，SysRole）` | `R<Void>` |
| DELETE | `/api/role/{ids}` | 删除角色 | `system:role:delete / admin` | `ids（路径变量）` | `R<Void>` |
| PUT | `/api/role/{id}/status` | 修改角色状态 | `system:role:edit / admin` | `id（路径变量）`<br>`body（body，Map<String, Integer>）` | `R<Void>` |
| PUT | `/api/role/{id}/menus` | 分配菜单权限 | `system:role:edit / admin` | `id（路径变量）`<br>`body（body，Map<String, Set<Long>>）` | `R<Void>` |
| GET | `/api/role/{id}/menus` | 获取角色菜单ID列表 | 仅登录 | `id（路径变量）` | `R<Set<Long>>` |
| GET | `/api/role/all` | 获取所有角色（下拉框用） | 仅登录 | - | `R<List<SysRole>>` |

## 菜单管理

`MenuController` · 前缀 `/api/menu`

| 方法 | 路径 | 说明 | 权限码 | 参数 | 返回 |
|---|---|---|---|---|---|
| GET | `/api/menu/tree` | 获取菜单树 | `system:menu:list / admin` | `keyword（query，可选）`<br>`status（query，可选）` | `R<List<SysMenu>>` |
| GET | `/api/menu/tree-select` | 获取菜单树（下拉选择用） | 仅登录 | - | `R<List<SysMenu>>` |
| GET | `/api/menu/user-menus` | 获取用户菜单（动态路由） | 仅登录 | `loginUser（登录用户，由框架注入）` | `R<List<SysMenu>>` |
| GET | `/api/menu/{id}` | 获取菜单详情 | `system:menu:query / admin` | `id（路径变量）` | `R<SysMenu>` |
| POST | `/api/menu` | 新增菜单 | `system:menu:add / admin` | `menu（body，SysMenu）` | `R<Void>` |
| PUT | `/api/menu/{id}` | 修改菜单 | `system:menu:edit / admin` | `id（路径变量）`<br>`menu（body，SysMenu）` | `R<Void>` |
| DELETE | `/api/menu/{id}` | 删除菜单 | `system:menu:delete / admin` | `id（路径变量）` | `R<Void>` |

## 部门管理

`DeptController` · 前缀 `/api/dept`

| 方法 | 路径 | 说明 | 权限码 | 参数 | 返回 |
|---|---|---|---|---|---|
| GET | `/api/dept/tree` | 获取部门树 | `system:dept:list / admin` | `keyword（query，可选）`<br>`status（query，可选）` | `R<List<SysDept>>` |
| GET | `/api/dept/tree-select` | 获取部门树（下拉选择用） | 仅登录 | - | `R<List<SysDept>>` |
| GET | `/api/dept/{id}` | 获取部门详情 | `system:dept:query / admin` | `id（路径变量）` | `R<SysDept>` |
| POST | `/api/dept` | 新增部门 | `system:dept:add / admin` | `dept（body，SysDept）` | `R<Void>` |
| PUT | `/api/dept/{id}` | 修改部门 | `system:dept:edit / admin` | `id（路径变量）`<br>`dept（body，SysDept）` | `R<Void>` |
| DELETE | `/api/dept/{id}` | 删除部门 | `system:dept:delete / admin` | `id（路径变量）` | `R<Void>` |

## 日志管理

`LogController` · 前缀 `/api/log`

| 方法 | 路径 | 说明 | 权限码 | 参数 | 返回 |
|---|---|---|---|---|---|
| GET | `/api/log/operation/page` | 分页查询操作日志 | `system:log:list / admin` | `pageNum（query，可选，默认 1）`<br>`pageSize（query，可选，默认 10）`<br>`keyword（query，可选）`<br>`module（query，可选）`<br>`startDate（query，可选）`<br>`endDate（query，可选）` | - |
| GET | `/api/log/login/page` | 分页查询登录日志 | `system:log:list / admin` | `pageNum（query，可选，默认 1）`<br>`pageSize（query，可选，默认 10）`<br>`keyword（query，可选）`<br>`startDate（query，可选）`<br>`endDate（query，可选）` | - |


---

# 附录 A：错误码

非 200 的业务码（`code` 字段），前端据此提示用户，**不要用 HTTP 状态码判断业务结果**。

| code | 含义 | 常见触发场景 |
|---|---|---|
| 200 | 成功 | — |
| 400 | 请求参数错误 | 参数缺失/格式错；敏感词 L1 命中；上传格式或大小不合规 |
| 401 | 未授权 | 未带 Token、Token 过期或已吊销 |
| 403 | 无权限 | Token 有效但缺少该接口要求的权限码 |
| 404 | 资源不存在 | 帖子已删除/未过审；文件不存在 |
| 500 | 服务器内部错误 | 未预期异常，应查日志 |
| 1001 | 用户名或密码错误 | 登录 |
| 1002 | 账号已被锁定 | — |
| 1003 | 账号已被禁用 | 管理端封号 |
| 1004 | Token 已过期 | 需重新登录 |
| 1005 | Token 无效 | 伪造/已拉黑 |
| 1006 | 原密码错误 | 修改密码 |
| 1007 | 验证码错误 | 管理端登录 |
| 1008 | 密码强度不足 | 需 8 位以上且含大小写字母、数字、特殊字符 |
| 1009 | 请求过于频繁 | 登录失败次数过多 |
| 1010 | 账号已被临时锁定 | 30 分钟后再试 |
| 2001 / 2002 | 角色名 / 角色编码已存在 | 角色管理 |
| 2003 | 角色已分配用户，无法删除 | 角色管理 |
| 3001 | 菜单名称已存在 | 菜单管理 |
| 4001 | 部门名称已存在 | 部门管理 |
| 4002 / 4003 | 部门存在子部门 / 存在用户，无法删除 | 部门管理 |
| 5001 | 数据已存在 | — |
| 5002 | 无权操作此数据 | 例如删除他人的帖子/评论（**不是 HTTP 403**） |

> ⚠️ 注意 `5002`：C 端删除他人内容返回的是 **HTTP 200 + code 5002**，
> 浏览器端必须以 `code` 为准。管理端权限不足则由框架直接返回 **HTTP 403**，两者语义不同。

---

# 附录 B：树洞管理权限码（v1.1 起）

改造前所有树洞管理端点都要求 `admin` 角色码，导致**只有超级管理员能管树洞**。
现在拆成下列细粒度权限码，并在判断里保留 `admin` 兼容。

| 模块 | 权限码 | 说明 |
|---|---|---|
| 首页 | `system:dashboard:view` | 管理端首页统计 |
| 用户管理 | `th:user:list` | 查看用户列表 |
| | `th:user:view` | 用户详情 / 行为日志 / 其发帖评论 |
| | `th:user:mute` | 禁言、解除禁言 |
| | `th:user:ban` | 封号、解封 |
| | `th:user:release` | 一键解除全部处罚 |
| | `th:user:violation` | 手动增减违规分 |
| 内容审核 | `th:moderation:list` | 看待审帖子/评论与统计 |
| | `th:moderation:audit` | 单条或批量通过/拒绝 |
| 帖子管理 | `th:post:list` / `th:post:view` | 列表 / 详情 |
| | `th:post:audit` / `th:post:pin` / `th:post:hide` / `th:post:delete` | 审核 / 置顶 / 隐藏 / 删除 |
| 评论管理 | `th:comment:list` / `th:comment:view` | 列表 / 详情 |
| | `th:comment:hide` / `th:comment:delete` | 隐藏 / 删除 |
| 举报管理 | `th:report:list` / `th:report:view` | 列表 / 详情 |
| | `th:report:handle` | 处理举报（成立会触发下架+计分+通知闭环） |
| 在线用户 | `th:online:list` / `th:online:kick` | 查看 / 强制下线 |
| 分类管理 | `th:category:list` / `th:category:add` / `th:category:edit` / `th:category:delete` | — |
| 公告管理 | `th:announcement:list` / `th:announcement:add` / `th:announcement:edit` / `th:announcement:delete` | — |
| 敏感词 | `th:sensitive:list` / `th:sensitive:add` / `th:sensitive:edit` / `th:sensitive:delete` / `th:sensitive:import` / `th:sensitive:refresh` | — |
| 数据分析 | `th:analytics:view` | 数据看板 |
| 站点配置 | `th:settings:view` / `th:settings:edit` | 读取 / 修改 |
| 操作日志 | `th:logs:view` | — |

## B.1 内置示例角色（见 `sql/migration_v1.3.0.sql`）

| 角色 | role_code | 能做什么 | 不能做什么 |
|---|---|---|---|
| 内容审核员 | `th_auditor` | 审核待审内容、处理举报、隐藏帖子和评论 | 删帖删评论、动用户、改站点配置、动词库 |
| 树洞运营 | `th_operator` | 分类、公告、数据看板、在线用户 | 一切内容处置、用户处罚、站点配置 |

演示账号 `th_auditor` / `th_operator`，**初始密码与种子管理员一致（`Admin@1234`）——上线前必须改密或删除**。

## B.2 授权链路

```
角色(role_code) ──┐
                  ├─→ authorities = 角色码 ∪ 菜单/按钮权限码 ─→ @PreAuthorize 判定
角色-菜单(sys_role_menu) ─→ 菜单 permission ──┘
```

- 给角色**分配菜单**既决定了它能看到哪些页面（`/api/menu/user-menus`），
  也决定了它拥有哪些权限码 —— 两者同源，不会出现"看得到页面但调不动接口"的错配。
- `admin` 角色码在所有判断里都放行（`stores/user.hasPermission` 与后端 `hasAnyAuthority` 双侧兼容）。

> ⚠️ `SecurityConfig` 中 `/api/admin/**` **只要求已登录**，具体权限一律交给方法上的 `@PreAuthorize`。
> 曾经这里是 `hasAuthority("admin")`，等于把整个 `/api/admin/**` 一刀切成超管专用，
> 方法级注解根本没机会生效 —— 这是"只有 admin 能管树洞"的根因之一。
> **新增管理端接口时务必补上 `@PreAuthorize`，否则默认所有登录用户都能访问。**

---

# 附录 C：头像上传

## C.1 完整流程

```
1) POST /api/file/avatar    （需登录，multipart/form-data，字段名固定为 file）
   → { "code":200, "data":"/api/file/avatar/3f1c....png" }

2) PUT /api/th/user/profile  { "avatar": "/api/file/avatar/3f1c....png" }

3) <img src="/api/file/avatar/3f1c....png">   （无需登录，浏览器直接渲染）
```

## C.2 约束

| 项 | 值 |
|---|---|
| 支持格式 | JPG / PNG / GIF / WebP（**按文件头魔数判定**，不看扩展名与 Content-Type） |
| 大小上限 | 2MB（业务校验），可通过 `FILE_AVATAR_MAX_SIZE` 调整 |
| 容器上限 | 10MB（`spring.servlet.multipart.max-file-size`），必须大于业务上限 |
| 存储位置 | `FILE_STORAGE_DIR`，默认 `./uploads`，按 `avatar/` 分目录 |
| 文件名 | **服务端生成**（UUID），客户端文件名一律丢弃 |
| 访问前缀 | `FILE_PUBLIC_PREFIX`，默认 `/api/file` |

## C.3 安全设计（每条都对应一类真实攻击）

- **拒绝 SVG**：SVG 可内嵌 `<script>`，作为图片直出即存储型 XSS。
- **魔数判类型**：把 `.txt` 改名成 `.png` 会被拒绝；伪造 Content-Type 也无效。
- **服务端生成文件名**：客户端传 `../../evil.png` 也只会在 `uploads/avatar/` 下落成随机名。
- **读取侧双重校验**：`category` 白名单 + 文件名严格正则（32 位 hex），归一化后再确认位于根目录内。
- **`avatar` 字段格式白名单**：只接受本站上传地址或 `http(s)://` 外链，
  防止 `javascript:` / `data:` 之类值进入 `<img src>`。
- **匿名不带头像**：匿名帖与匿名评论的 `authorAvatar` 恒为空 —— 头像可反向识别身份。

> 📌 **读取接口是公开的**：`<img>` 标签不会携带 `Authorization` 头，若要求鉴权头像永远渲染不出来。
> 安全性由"不可枚举的随机文件名"保证。**不要把敏感文件放进这个目录。**

## C.4 前端约定

- 上传时**不要手动设置 `Content-Type`**，交给浏览器带 boundary，否则后端解析失败。
- 前端已做一次类型/大小预校验（省掉必然失败的往返），但**唯一可信的校验在后端**。
- 头像缓存：登录接口只返回 `token/nickname`，头像需额外调 `GET /api/th/auth/user-info` 获取。

---

# 附录 D：数据权限（行级隔离）

角色上的 `data_scope` 决定登录用户能看到哪些部门的数据，由 MyBatis-Plus
`DataPermissionInterceptor` 自动为 `sys_user` / `sys_dept` 的查询追加条件。

| 值 | 含义 | 边界依据 |
|---|---|---|
| 1 | 全部数据 | 不限制 |
| 2 | 本集团及以下 | 组织树根节点 |
| 3 | 本公司及以下 | `ancestors` 中第 2 层节点 |
| 4 | 本部门及以下 | 当前部门及其全部子部门 |
| 5 | 本部门及以下（限 N 级） | 向下只看 N 层，N 存于 `sys_role.data_scope_level` |
| 6 | 本部门 | 不含子部门 |
| 7 | 自定义部门 | `sys_role_dept` 中勾选的部门（不含其子部门） |
| 8 | 仅本人 | `sys_user` 恒保留"本人可见" |

- 可见部门集合在**登录时预计算**并写入 `LoginUser`，避免每条 SQL 递归组织树。
- `sys_dept` 查询用的是「可见部门 + 其全部祖先」，否则父节点被过滤会导致组织树在前端整棵消失；
  `sys_user` 用的是**精确的可见部门集合**，不含祖先，避免放大可见范围。
- 解析失败时降级为「仅本人」（最保守），不会放大权限。

> ⚠️ 拦截器**只改写 SELECT**，`UPDATE` / `DELETE` 不在其覆盖范围内。
> 写侧防护由 **`DataScopeGuard`**（Service 层守门员）承担：所有写方法进入业务逻辑前先断言归属，
> 判定逻辑**委托 `DataScopeHelper` 的读侧规则**，保证「读看不见 ⇒ 写也写不了」。
> 另有内置超管账号保护（除本人外任何人不得改/删/禁用/重置 id=1），避免系统被越权者锁死。
>
> 🔑 **豁免清单**：守门员需要"绕过数据范围"读一次目标行，才能区分 404（不存在）与 403（越权）。
> 若这次读取也被拦截器改写，两种场景都会变成 0 行。因此 `DataPermissionHandler`
> 按 `mappedStatementId` 精确豁免 `selectRawById` / `selectRoleCodesByUserId` /
> `selectPermissionsByUserId`（仅判定与登录装载用，**业务查询严禁走豁免路径**）。
> 豁免匹配用方法名末段精确比对，不做前缀匹配（防 `selectRawByIdList` 之类误放行）。

---

# 附录 E：本版接口变更（树洞 v1.1）

## 新增

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/file/avatar` | 上传头像（登录） |
| GET | `/api/file/{category}/{filename}` | 读取文件（公开，`category` 目前仅 `avatar`） |
| DELETE | `/api/th/comment/{id}/like` | **取消点赞评论**（此前只有点赞、无法取消，是接口缺口） |

## 变更

| 接口 | 变更 | 原因 |
|---|---|---|
| `GET /api/th/comment/page` | 新增可选登录态；响应新增 `liked`、`authorAvatar` | 前端只能靠本地猜点赞态，刷新即丢 |
| `GET /api/th/post/{id}` | 改为走 `getPostDetail`，正确填充 `authorName` / `authorAvatar` / `categoryName` | 原实现直接用 `getById`，**实名帖的 authorName 恒为 null，详情页一直显示"未知用户"** |
| `GET /api/th/post/page` 等列表 | 响应新增 `authorAvatar` | 头像展示 |
| 全部树洞管理接口 | `@PreAuthorize` 由 `admin` 拆为细粒度权限码（保留 `admin`） | 支持按角色授权 |
| `SecurityConfig` | `/api/admin/**` 由 `hasAuthority('admin')` 改为 `authenticated()` | 原来是一道全局硬门，非 admin 角色必然 403 |
| `SecurityConfig` | `GET /api/file/**` 加入白名单；`DELETE /api/th/comment/{id}/like` 纳入需登录列表 | 头像可渲染 / 取消点赞可用 |
| `GET /api/th/auth/user-info` | 沿用原有结构（已含 `avatar`） | — |

## 接口对账结论

用 `scripts/api_audit.py` 把后端全部端点与两个前端的调用逐条比对：

- **前端调用 → 后端端点：全部命中**，没有调用不存在的接口。
- **后端端点 → 前端调用：修复前有 4 个从未被调用**，其中 2 个是真问题：
  - `GET /api/th/post/{id}/liked` —— C 端从未调用，导致刷新后"已抱抱"状态丢失，
    且点赞数会被本地乐观自增算错（服务端幂等直接返回，前端却仍加 1）。**已接入。**
  - `PUT /api/admin/th/post/{id}/audit` —— 管理端单条审核实际走的是 `batch-audit`，
    属冗余端点，保留但不再使用。
  - `GET /api/test/ping`、`GET /api/test/admin-ping` —— 连通性自测端点，保留。

---

# 附录 F：验证状态总览（2026-09-24）

> 用途：标记哪些功能已通过真实环境验证。**已标记 ✅ 的模块，后续改动未触及前不必重复测试。**
> 触及 = 改动了该模块的代码或其依赖的表结构/配置。

## ✅ 已验证通过（可直接采信）

| 模块 | 验证依据 | 结果 | 日期 |
|---|---|---|---|
| 内容治理（敏感词/禁言/举报/限额） | 后端单测 61 个 | 全绿 | 09-24 |
| 数据权限 8 级（SELECT 改写） | `DataScopeHelperTest` + 接口级 E2E 77 用例 | 全绿 | 09-24 |
| 头像上传全链路（上传/恶意文件/穿越/匿名脱敏） | `scripts/verify_avatar_perm.sh` | 65/65 | 09-24 |
| 树洞管理权限矩阵（审核员/运营正反用例） | `scripts/verify_avatar_perm.sh` | 65/65 | 09-24 |
| 帖子/评论点赞态与计数一致性 | `scripts/verify_avatar_perm.sh` + 浏览器 E2E | 通过 | 09-24 |
| C 端全站页面与交互（含头像展示、消息中心） | `scripts/verify_treehole_ui.cjs`（真实浏览器） | 76/76，控制台错误 0 | 09-24 |
| 登录页整屏分屏布局（深/浅主题、移动端） | `scripts/verify_treehole_ui.cjs` §11 + 截图 | 通过 | 09-23 |
| 前端接口调用与后端端点一致性 | `scripts/api_audit.py` 双向对账 | 136 调用全命中 | 09-24 |
| 角色创建 / 菜单分配 / 用户分配角色（管理端 UI） | `scripts/verify_admin_role_ui.cjs`（真实浏览器+真实验证码登录） | 10/10，控制台错误 0 | 09-24 |
| 防提权：创建 role_code=admin 被拒（浏览器实测） | 同上 | 通过 | 09-24 |
| **C 端用户视角 DOM 巡检**（白屏/文案/按钮可见可点/破图/横向溢出/JS 报错） | `scripts/audit_dom_user_view.cjs`（真实浏览器） | **73/73**，控制台错误 0 | 09-24 |
| **管理端用户视角 DOM 巡检**（登录页 + 8 个系统页 + 12 个树洞管理页） | `scripts/audit_dom_admin.cjs`（真实浏览器，逐路由） | **120/120**，控制台错误 0 | 09-24 |
| 注册头像占位缺陷（首页 3 张破图） | 同上巡检暴露 → 修复后复测无破图 | 已修复 | 09-24 |
| 管理端登录页横向溢出（800px） | 同上巡检暴露 → `.login-container{overflow:hidden}` | 已修复，复测溢出=0 | 09-24 |
| **越权写防护**（改/删/禁用/重置密码/分配角色 × 用户与部门） | `scripts/verify_write_scope.cjs`（真实后端、真实越权账号） | **26/26**，全部返回 5002 | 09-24 |
| **内置超管账号保护**（他人不得改/删/禁用/重置 id=1） | 同上 | 通过，提示"内置超级管理员账号不可由他人操作" | 09-24 |
| **越权读语义修正**（越权从 200+null 改为明确 5002，不存在为 404） | 同上 + `DataScopeWriteGuardTest` | 通过 | 09-24 |
| **守门员不误伤**（admin 仍可管理他人；主管可管本部门；自身可改自己） | `scripts/verify_guard_no_overreach.cjs` | **16/16** | 09-24 |
| **读写规则一致性**（42 组合矩阵：读不可见 ⇒ 写不可写；可写 ⇒ 可读） | `DataScopeWriteGuardTest`（单测） | 通过 | 09-24 |
| **拦截器豁免清单**（守门员判定语句不被追加数据范围；业务查询仍被拦截） | `DataPermissionBypassTest`（单测） | 11/11 | 09-24 |
| 后端单测总量 | `mvn -pl permission-system -am test` | **84/84 全绿** | 09-24 |

## ⚠️ 待验证 / 待升级

| 项 | 状态 | 说明 |
|---|---|---|
| 写操作数据范围校验（UPDATE/DELETE） | ✅ **已升级（09-24）** | 拦截器仍只改写 SELECT（不宜改动，风险高）；写操作改由 `DataScopeGuard` 在 Service 层显式校验，**读写同源**（判定逻辑委托 `DataScopeHelper` 的读侧规则）。已通过 26/26 攻击验证 + 16/16 不误伤验证 + 单测锁死 |
| 管理端浏览器 E2E 重写 | 待升级 | `browser_e2e.cjs` 选择器过时（已 gitignore）；现有覆盖已由 `audit_dom_admin.cjs` + `verify_admin_role_ui.cjs` 替代 |
| C 端 `tsconfig.json` 类型检查 | 待补 | build 只剥类型不做检查 |
| 管理端 `tsconfig.json` 类型检查 | 待补 | 同上 |
| 《数据权限》附录 D 的"已知限制"段落 | 已更新 | 写侧兜底已从"依赖接口层权限码"升级为守门员强制校验 |
